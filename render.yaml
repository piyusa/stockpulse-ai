services:
  - type: web
    name: stockpulse-ai
    runtime: ruby
    buildCommand: bundle install
    startCommand: ruby app.rb
    envVars:
      - key: USERS_FILE
        value: /tmp/stock_users.json
      - key: GROQ_API_KEY
        sync: false
